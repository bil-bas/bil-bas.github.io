%article

  - if @summary
    %h3
      %a{href: @item.path}  #{@item[:title]} - #{pretty_date @item[:created_at]}

    = @item.summary
    %a{ href: @item.path } Read more...

    %p.tags #{@site.tags_for(@item, base_url: '/blog/tags/')}

  - else
    %p.posted= "Posted: #{pretty_date @item[:created_at]}"
    = yield

    %p.tags #{@site.tags_for(@item, base_url: '/blog/tags/')}

    = render "comments", item: @item

